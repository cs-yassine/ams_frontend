import { BeginWithUpper2Pipe } from './begin-with-upper2-pipe';

describe('BeginWithUpper2Pipe', () => {
  it('create an instance', () => {
    const pipe = new BeginWithUpper2Pipe();
    expect(pipe).toBeTruthy();
  });
});
