import { BeginWithUpperPipe } from './begin-with-upper-pipe';

describe('BeginWithUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new BeginWithUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
